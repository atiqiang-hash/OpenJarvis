{
  "name": "nabila-frontend",
  "private": true,
  "version": "0.8.0",
  "type": "module",
  "description": "NABILA — ATI's personal AI agent. Rewritten UI (drop-in for the v0.7.3 FastAPI backend).",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --port 5555"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
